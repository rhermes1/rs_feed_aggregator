<% provide(:title, 'Tags') %>
<div class="news">
  <div class="new_contact">
    <%= render 'form' %>
  </div>
  <% if Tag.any? %>
    <div class="new_contact">
      <%= render 'alltags' %>
    </div>
  <% end %>
  <%= link_to 'Back', root_path %>
</div>

